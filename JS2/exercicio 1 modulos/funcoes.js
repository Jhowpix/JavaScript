
export function soma(num){
    let result = 0;
    result += (2 * 8);
    num = result
    return num
}

export function areaQuad(area){
    let quadrado = 0;
    quadrado += (4 * 2);
    area = quadrado
    return area
}